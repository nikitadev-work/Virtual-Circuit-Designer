version: "3.8"

services:
  frontend:
    build:
      context: react
      dockerfile: Dockerfile
    restart: always
    networks:
      - app-network

  # nginx:
  #   build: nginx
  #   restart: always
  #   ports:
  #     - "80:80"
  #   depends_on:
  #     - frontend
  #   networks:
  #     - app-network

networks:
  app-network:
    external: true
    name: backend_app-network