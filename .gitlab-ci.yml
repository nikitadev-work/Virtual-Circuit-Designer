stages:
  - build
  - deploy

build:
  image: node:16
  stage: build
  script:
    - npm install
    - npm run build
    - npm run export
  artifacts:
    paths:
      - out

pages:
  stage: deploy
  dependencies:
    - build
  script:
    - mv out public
  artifacts:
    paths:
      - public
  only:
    - testFront
